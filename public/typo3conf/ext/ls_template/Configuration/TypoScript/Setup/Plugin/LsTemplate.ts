plugin.ls_template {
    settings {
        headerLogoPath = {$plugin.ls_template.settings.headerLogoPath}
        sitetitle = {$plugin.ls_template.settings.sitetitle}

        cartImage = {$plugin.ls_template.settings.cartImage}
        cartImageTitle = {$plugin.ls_template.settings.cartImageTitle}

        pageUid {
            home = {$plugin.ls_template.settings.pageUid.home}
            cart = {$plugin.ls_template.settings.pageUid.cart}
        }
    }
}